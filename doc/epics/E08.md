## Objetivo
Una URL pública que funcione. Se arranca el día 1 con un esqueleto desplegado, no el último día.

## Alcance
- [ ] Dockerfile multi-stage sobre `eclipse-temurin:25` (JDK para build, JRE para runtime)
- [ ] Perfil `prod` con configuración por variables de entorno
- [ ] Proyecto en Railway con addon de Postgres
- [ ] Secretos como variables de entorno: API key del modelo, secreto del JWT, credenciales de base
- [ ] Deploy automático desde `main`
- [ ] `/actuator/health` y verificación de arranque en frío
- [ ] Rate limit en `/api/chat` para proteger la API key
- [ ] Runbook de despliegue y de rotación de secretos en `doc/`

## Criterio de aceptación
Alguien con solo la URL y las credenciales del enunciado completa una reserva. Ningún secreto está en el repositorio.

## Trazabilidad
RT-09

## Dependencias
E00 para el esqueleto inicial; E06 para el cierre.

## Rama
`feature/E08-deploy`
