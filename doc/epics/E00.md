## Objetivo
Dejar el repositorio listo para que cada épica siguiente sea un flujo de trabajo limpio y no una improvisación.

## Alcance
- [ ] Instalar JDK 25 y fijarlo en `pom.xml` (en esta máquina hay JDK 24)
- [ ] Proyecto Maven con Maven Wrapper (`./mvnw`) y Spring Boot 3.5.x
- [ ] Estructura de paquetes hexagonal: `domain` / `application` / `infrastructure`
- [ ] Protección de ramas `main` y `develop`: PR obligatorio, CI en verde
- [ ] Plantillas de issue (épica y tarea) y de PR en `.github/`
- [ ] GitHub Actions: build + tests en cada PR contra `develop`
- [ ] Spotless o Checkstyle y `.editorconfig`
- [ ] Estructura `doc/adr/` con ADR-0001 (stack elegido)
- [ ] Verificar temprano el kernel Java de Jupyter (`rapaio-jupyter-kernel`) contra JDK 25 — decide el plan del notebook en E09

## Criterio de aceptación
Un PR de prueba contra `develop` corre CI en verde y no se puede mergear sin ella.

## Trazabilidad
Habilitador de todas las épicas. No cubre requisitos del enunciado directamente.

## Dependencias
Ninguna.

## Rama
`chore/E00-baseline`
