Parte de #9.

## Qué hay que hacer
- Dockerfile multi-stage: `eclipse-temurin:25` JDK para compilar, JRE para runtime
- Cache de dependencias Maven entre builds
- Perfil `prod` con toda la configuración por variables de entorno
- Usuario no root en el contenedor

## Criterio de aceptación
`docker build` y `docker run` levantan la app contra un Postgres local sin ningún archivo de configuración con secretos.

## Rama
`feature/E08.1-dockerfile-prod-profile`
