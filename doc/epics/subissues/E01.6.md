Parte de #2.

## Qué hay que hacer
- Jerarquía `sealed` de `BookingError`, un subtipo por tipo de violación: capacidad excedida, slot ocupado, duración máxima, rango no contiguo, fuera de horario, título faltante, en el pasado
- Cada subtipo lleva los datos de la causa, no solo un mensaje: capacidad real, slots en conflicto, límite superado
- Cálculo de disponibilidad: dada una sala y un rango, devolver qué slots están libres y cuáles ocupados

## Por qué importa el detalle de los errores
Es lo que después permite que el agente de #6 responda «la C está ocupada de 10:30 a 11:00, la D queda libre todo el rango» en vez de «no se pudo».

## Criterio de aceptación
Un `switch` con pattern matching sobre `BookingError` es exhaustivo sin rama `default`. El cálculo de disponibilidad tiene test con reservas parciales en el medio del rango.

## Cubre del checklist de #2
BookingError sellado, cálculo de disponibilidad.

## Rama
`feature/E01.6-errors-availability`
