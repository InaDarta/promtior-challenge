Parte de #6.

## Qué hay que hacer
- `createBooking` — RT-03, asociada siempre al usuario del token
- `cancelBooking` — RT-06, solo reservas propias

Los errores del dominio se devuelven al modelo como resultado estructurado, no como excepción: el modelo necesita leer la causa para poder explicarla.

## Criterio de aceptación
El usuario del token es el propietario de la reserva creada, independientemente de lo que diga el texto del mensaje. Una violación de regla devuelve el código de error de #5 y **ninguna** reserva queda creada.

## Rama
`feature/E05.5-write-tools`
