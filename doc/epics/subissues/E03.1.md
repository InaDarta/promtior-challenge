Parte de #4.

## Qué hay que hacer
- Configuración de Spring Security
- `POST /api/auth/login` que valida credenciales y devuelve un JWT
- `PasswordEncoder` BCrypt
- Resto de la API autenticada; login y estáticos públicos

## Criterio de aceptación
Login con User1 y el password del enunciado devuelve un token válido. Cualquier endpoint de reservas sin token devuelve 401.

## Rama
`feature/E03.1-jwt-login`
