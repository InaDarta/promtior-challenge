Parte de #2.

## Qué hay que hacer
- `Room`: identificador A–E y capacidad. Catálogo cerrado, no editable por el usuario
- `TimeSlot`: bloque de 30 minutos, con validación de alineación a `:00` o `:30`
- `BookingRange`: rango de slots **contiguos**, con inicio y fin alineados
- Todo como `record`, sin dependencias de framework

## Criterio de aceptación
Construir un `TimeSlot` a las 10:17 falla. Construir un `BookingRange` con un hueco entre slots falla. Los tests no importan nada de Spring.

## Cubre del checklist de #2
Room/TimeSlot/BookingRange, regla RN-02.

## Rama
`feature/E01.1-value-objects`
