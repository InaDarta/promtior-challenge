Parte de #4.

## Qué hay que hacer
- Password incorrecta devuelve 401 sin filtrar si el usuario existe
- Token vencido o ausente devuelve 401
- Cancelar una reserva de otro usuario devuelve 403
- Test de suplantación: una petición cuyo contenido pide actuar como User2 se ejecuta igual con la identidad del token

## Criterio de aceptación
Los cuatro escenarios tienen test. El de suplantación queda escrito de forma que siga aplicando cuando en #6 el que invoque sea el agente.

## Rama
`feature/E03.3-authorization-tests`
