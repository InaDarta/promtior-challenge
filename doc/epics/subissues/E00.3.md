Parte de #1.

## Qué hay que hacer
- Workflow de GitHub Actions que corre `./mvnw verify` en cada PR contra `develop`
- Cache de dependencias Maven
- Protección de `main` y `develop`: PR obligatorio, CI en verde para mergear

## Criterio de aceptación
Un PR de prueba contra `develop` dispara CI y no se puede mergear hasta que pase.

## Rama
`chore/E00.3-ci-branch-protection`
