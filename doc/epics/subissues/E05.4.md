Parte de #6.

## Qué hay que hacer
Tools de solo lectura, como adaptadores finos sobre los casos de uso de #5. **Sin lógica propia.**

- `listAvailableRooms` — RT-04
- `getRoomSchedule` — RT-05
- `listMyBookings` — habilita cancelar en la práctica

Descripciones y esquemas estrictos: tipos acotados, enum para la sala, formato explícito de fecha y hora.

## Criterio de aceptación
Cada tool delega en su caso de uso y no toca repositorios ni reglas. El esquema no admite una sala fuera de A–E.

## Rama
`feature/E05.4-query-tools`
