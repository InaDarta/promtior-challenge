Parte de #5.

## Qué hay que hacer
- `ListAvailableRooms`: salas libres en un rango, filtrando además por capacidad mínima si se indica
- `GetRoomSchedule`: slots libres y ocupados de una sala en un rango
- `ListMyBookings`: reservas del usuario autenticado

## Sobre ListMyBookings
No está pedido en el enunciado. Se agrega porque sin él cancelar es impracticable: el usuario no sabe qué reservas tiene ni con qué identificador referirse a ellas.

## Criterio de aceptación
Los tres casos de uso se apoyan en el cálculo de disponibilidad de #21 y no reimplementan lógica de solapamiento.

## Rama
`feature/E04.1-query-use-cases`
