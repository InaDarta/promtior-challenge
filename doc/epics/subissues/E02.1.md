Parte de #3.

## Qué hay que hacer
- Migración Flyway `V1__schema.sql`: tablas `room`, `app_user`, `booking`
- Entidades JPA y mapeo hacia y desde el modelo de dominio — el dominio no se contamina con anotaciones de persistencia
- Adaptadores que implementan los puertos de repositorio definidos en #2
- Configuración de Testcontainers con Postgres para los tests de integración

## Criterio de aceptación
La app arranca contra un Postgres real y Flyway aplica la migración. Los tests de repositorio corren sobre Testcontainers, no sobre H2.

## Rama
`feature/E02.1-flyway-jpa`
