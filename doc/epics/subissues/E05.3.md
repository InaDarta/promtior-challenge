Parte de #6.

## Qué hay que hacer
- `BookingAssistant` como `AiService` de LangChain4j
- `ChatMemory` por sesión, con ventana acotada
- `POST /api/chat`: recibe el mensaje, resuelve el usuario desde el token, responde
- La sesión de conversación queda atada al usuario autenticado

## Criterio de aceptación
Dos usuarios distintos tienen conversaciones independientes. El historial permite que «cancelala» en el segundo turno se refiera a lo hablado en el primero.

## Rama
`feature/E05.3-assistant-chat-endpoint`
