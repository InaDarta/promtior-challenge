Parte de #1.

## Qué hay que hacer
Spike de de-riesgo, con resultado documentado y no con código productivo.

- Instalar `rapaio-jupyter-kernel` y probarlo contra el JDK 25 recién instalado
- Verificar que la magia de dependencias Maven resuelve un artefacto de LangChain4j
- Ejecutar una celda que instancie un `ChatModel` y haga una llamada trivial

## Por qué ahora y no en E09
El enunciado da por sentado Python. Si el kernel Java no funciona con JDK 25, el plan B es un notebook Python contra la API desplegada, con el código Java en celdas de markdown — y esa decisión hay que tomarla temprano, no el último día.

## Criterio de aceptación
Decisión escrita en `doc/adr/`: notebook en Java o notebook en Python, con el motivo. Si es Java, queda una celda de prueba que corre.

## Rama
`chore/E00.5-jupyter-kernel-spike`
