Parte de #5.

## Qué hay que hacer
- `CreateBooking`: valida contra el dominio, persiste, y asocia la reserva al usuario autenticado
- `CancelBooking`: solo reservas propias; ajena devuelve error de autorización
- Límites transaccionales correctos alrededor de la escritura

## Criterio de aceptación
Crear una reserva que viola cualquier regla de #2 no deja rastro en la base. Cancelar una reserva ajena falla sin revelar si existe.

## Rama
`feature/E04.2-write-use-cases`
