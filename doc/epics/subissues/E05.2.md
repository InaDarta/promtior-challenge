Parte de #6.

## Qué hay que hacer
- Un bean `ChatModel` por perfil de Spring: `gemini` (primario), `groq` (respaldo), `ollama` (offline)
- Gemini vía `langchain4j-google-ai-gemini`; Groq vía el cliente OpenAI-compatible apuntando a `https://api.groq.com/openai/v1`
- Configuración por variables de entorno, sin keys en el repo

## Criterio de aceptación
Cambiar de proveedor es cambiar el perfil activo. Ninguna clase fuera de la configuración menciona un proveedor concreto.

## Rama
`feature/E05.2-model-provider`
