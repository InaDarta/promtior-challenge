/**
 * Adaptadores REST: controladores HTTP y sus DTOs de entrada y salida.
 *
 * <p>Subpaquetes por responsabilidad: {@code controller} (los {@code @RestController}), {@code dto}
 * (request/response) y {@code error} (los {@code @ExceptionHandler} que traducen errores de dominio
 * y de la app a {@code problem+json}, contrato de E04.4).
 */
package com.promtior.booking.infrastructure.rest;
